# Vowser-Backend 서버의 주소를 설정합니다.
# 이 파일을 복사하여 config.properties 파일을 생성하고, 실제 서버 주소로 수정하여 사용하세요.
backend.url=http://localhost:8080

# Optional: Default browser zoom (page scale). 1.0 = 100%, 1.25 = 125%
# Applies per page using DevTools; if unset, defaults to 1.0
# Note: If you also set chromium.deviceScaleFactor, the effects may compound.
# browser.zoom=1.25



# Optional: Force device scale factor for Chromium UI at launch.
# This scales the entire Chromium window (toolbars as well). Example: 1.25
# chromium.deviceScaleFactor=1.25

# Optional: Start Chromium in fullscreen mode
# chromium.startFullscreen=true

# Optional: Fit page zoom dynamically to window size (overrides browser.zoom)
# browser.fitToWindow=true

# Optional: Start Chromium with a specific window size (e.g., 1920x1080)
# chromium.windowSize=1920x1080
